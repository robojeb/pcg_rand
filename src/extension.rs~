

pub trait ExtAmount {
    fn ext_size() -> usize;
    fn ext_bits() -> usize;
}

struct Ext1024;

impl ExtAmount for Ext1024 {
    fn ext_size() -> usize {
        1024
    }
    fn ext_bits() -> usize {
        10
    }
}

pub struct ExtPcg {

}
